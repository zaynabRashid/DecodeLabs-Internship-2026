# Random Password Generator

## Project 3 – Python Programming

This project is developed as part of the **DecodeLabs Industrial Training Program 2026**.

The Random Password Generator is a Python-based application that generates random passwords according to the length specified by the user. The project demonstrates the use of Python modules, random selection, string manipulation, and user input.

## Objective

The main objective of this project is to create a program that asks the user for a password length and generates a random password using letters and numbers.

The project focuses on:

- Importing and using Python modules
- Random character selection
- String manipulation
- User input and validation
- Generating passwords of a specified length

## Features

### Custom Password Length
- Allows the user to enter the desired password length.
- Generates a password according to the specified length.
- Requires a minimum length of 8 characters.

### Random Password Generation
- Generates a different password using random character selection.
- Uses Python's `random` module.

### Letters
- Includes both uppercase and lowercase letters.
- Uses `string.ascii_letters`.

### Numbers
- Includes numeric characters from `0` to `9`.
- Uses `string.digits`.

### Special Characters
- Includes special characters such as `@`, `#`, `$`, `%`, `^`, `&`, `*`, `!`, and `?`.
- Provides additional password complexity.

### Input Validation
- Checks whether the entered password length is a valid number.
- Prevents passwords shorter than 8 characters.

## Python Modules Used

### `random`

The `random` module is used to randomly select characters from the available character pool.

### `string`

The string module provides predefined groups of characters.
- string.ascii_letters
- string.digits

## How to Run
Python 3.13 must be installed on the system.
Open the project directory in Visual Studio Code and run:
python Password.py

## Internship Project

Program: DecodeLabs Industrial Training Program
Batch: 2026
Project: Project 3 – Random Password Generator
Track: Python Programming




